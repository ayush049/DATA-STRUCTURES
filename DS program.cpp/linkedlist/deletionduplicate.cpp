// Deletion of duplicate nodes
#include "ll.h"
struct node * dd(struct node** list,int x){
    struct node *p,*q;
    p=*list;
    while(p!=NULL){
        if(p->info==x){
            p=p->next;
            if(q==NULL)
                deletion
        }
    }
}